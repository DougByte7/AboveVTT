{
  "name": "abovevtt",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the AboveVTT browser extension. No runtime dependencies — see docs/index.md for how to load the extension locally.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "^9.19.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
